<?xml version="1.0"?>
<configuration>
	<connectionStrings>
		<!-- please adjust the connection string embedded in the element below to target the proper catalog / server using the proper user / password combination -->
		<add name="ConnectionString.SQL Server (SqlClient)" connectionString="data source=LLBLGen5Dev\SQLEXPRESS2016;initial catalog=OrmCookbook;integrated security=SSPI;persist security info=False;packet size=4096"/>
	</connectionStrings>
</configuration> 